# This file is now split into table_manager.py, form_schema.py, and form_utils.py
# Please update your route imports accordingly.

# Example:
# Instead of: from models.form import add_form, create_form_table
# Use:        from models.table_manager import add_form
# Use:        from models.form_schema import create_form_table

pass # Keep the file temporarily to avoid import errors until routes are updated